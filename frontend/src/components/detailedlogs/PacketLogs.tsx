export default function PacketLogs() {
    return (
        <div className="packet-logs">
        <h2>Packet Logs</h2>
        <p>This is where the packet logs will be displayed.</p>
        </div>
    );
}